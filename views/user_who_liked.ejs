<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Likes List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .likes-container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            width: 80%;
            max-width: 600px;
        }

        .likes-container h1 {
            margin-bottom: 20px;
            color: #333;
        }

        .likes-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .like-item {
            display: flex;
            align-items: center;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }

        .like-item:last-child {
            border-bottom: none;
        }

        .user-photo {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 15px;
        }

        .user-name {
            font-size: 18px;
            color: #333;
        }

        .back-to-post {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 15px;
            color: #007bff;
            text-decoration: none;
            border: 1px solid #007bff;
            border-radius: 4px;
            transition: background-color 0.3s, color 0.3s;
        }

        .back-to-post:hover {
            background-color: #007bff;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="likes-container">
        <h1>Users who liked this post</h1>
        <ul class="likes-list">
            
          <% if(data.likes){ %>
           <% data.likes.forEach((element)=>{ %>

            <li class="like-item">
                <img src="/images/usersdp/<%= element.dp %>" alt="User 1" class="user-photo">
                <a href="profile/<%=element._id%>" class="user-name">@<%= element.user_name %></a>
                <!-- <span class="user-name"><%=element.user_name%></span> -->
            </li>
                
          <%  }) %>
          <%}%>
        </ul>
        <a href="/" class="back-to-post">Back to Post</a>
    </div>
</body>
</html>
